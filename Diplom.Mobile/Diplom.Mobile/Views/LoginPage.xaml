<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Diplom.Mobile.Views.LoginPage"
             Title="Авторизация">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="7*" />
        </Grid.RowDefinitions>

        <StackLayout Grid.Column="0" Grid.Row="1" Margin="20">
            <Entry x:Name="loginEntry" Placeholder="Логин" />
            <Entry x:Name="passwordEntry" Placeholder="Пароль" IsPassword="True" />

            <Button Text="Войти" Clicked="Button_Clicked" Margin="40, 10" Style="{StaticResource ButtonShop}" />
            <Label Text="Нет аккаунта? Зарегистрируйтесь!" HorizontalOptions="Center" Style="{StaticResource labelTextDescription}" />
            <Button Text="Регистрация" Clicked="Button_Clicked_1" Margin="40, 10" Style="{StaticResource ButtonShop}" />
            <!--<Button Text="жмак" Clicked="Button_Clicked_2" Margin="40, 10"/>-->
        </StackLayout>
    </Grid>
</ContentPage>